/var/log/nginx/*.log
{
   daily
   rotate 7
   dateext
   create 666 root root
   missingok
   compress
   notifempty
   sharedscripts
   postrotate
      if [ -f /var/run/nginx.pid ]; then
         kill -USR1 `cat /var/run/nginx.pid`
      fi
   endscript
}